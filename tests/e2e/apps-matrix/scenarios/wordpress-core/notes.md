# wordpress-core

Focus:

- baseline permalink/front-controller behavior
- deny rules for sensitive files
- no-index directory access
- proof that `.htaccess` is actually loaded

Expected long-term artifacts:

- `final.htaccess`
- per-engine header dump
- per-engine probe output
- summary row for each case
